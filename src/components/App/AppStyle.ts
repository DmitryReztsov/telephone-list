import {flex, flexbox} from '../../style/templates/flex';

export const app = {
  ...flex(),
  ...flexbox('center'),
};


export const appContainer = {
  ...flex(),
  px: '27.5px',
  maxWidth: '1200px',
};
